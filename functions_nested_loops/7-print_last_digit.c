#include "main.h"

/**
 * print_last_digit - prints the last digit of a number
 *
 * @n: number to ve proccessed
 * Return: 0
 */
int _abs(int n)
{
	int d = (n % 10) + 48;
	
	_putchar(d);
	return (0);
}
